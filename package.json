{
  "name": "@vadistic/tsask",
  "version": "0.2.1",
  "private": false,
  "description": "tsask = 'typescript task'",
  "repository": {
    "type": "git",
    "url": "https://github.com/vadistic/tsask"
  },
  "license": "MIT",
  "author": "Jakub Wadas <vadistic@gmail.com>",
  "main": "dist/index.js",
  "bin": {
    "tsask": "dist/runner/cli.js"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.build.json",
    "clean": "rm -rf dist",
    "cli": "node dist/runner/cli.js",
    "dev": "node src/runner/cli.js",
    "format": "prettier --write src/**/*.ts",
    "lint": "eslint src/**/*.ts",
    "prepublishOnly": "yarn format && yarn lint && yarn clean && yarn build"
  },
  "dependencies": {
    "case": "^1.6.3",
    "cosmiconfig": "^6.0.0",
    "fs-extra": "^9.0.0",
    "globby": "^11.0.0",
    "open": "^7.0.3",
    "sort-package-json": "^1.40.0",
    "ts-node": "^8.8.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.1.0",
    "@vadistic/eslint-config-base": "^0.2.0",
    "@vadistic/prettier-config": "^0.2.0",
    "eslint": "^6.8.0",
    "eslint-plugin-prettier": "^3.1.2",
    "prettier": "^2.0.2",
    "typescript": "^3.8.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
